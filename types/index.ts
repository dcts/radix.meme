export type TTokenData = {
  address: string;
  progress: number; // ready-to-dexter
  name: string;
  symbol: string;
  imageUrl: string;
  description: string;
  telegramUrl?: string;
  xUrl?: string;
  website?: string;
};
