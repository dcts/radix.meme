export type TTokenData = {
  address: string;
  progress: number; // ready-to-dexter
  name: string;
  symbol: string;
  iconUrl?: string;
  imageUrl: string;
  description: string;
  telegramUrl?: string;
  xUrl?: string;
  website?: string;
  supply?: number;
  maxSupply?: number;
  lastPrice?: number;
};
