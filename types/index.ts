export type TTokenData = {
  id: string;
  address: string;
  name: string;
  symbol: string;
  imageUrl: string;
  description: string;
  telegramUrl?: string;
  xUrl?: string;
  website?: string;
};
