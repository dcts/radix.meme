// import {
//   calcBuyTokensSpecified,
//   calcBuyXrdSpecified,
//   calcSellTokensSpecified,
//   calcSellXrdSpecified,
// } from "./price-calcs";

describe("calcBuyTokensSpecified", () => {
  it("should do xyz", () => {
    // Add tests
    // expect(result).toBe(expectation);
    // expect(calcBuyTokensSpecified(100, 1000, 10)).toBe(123);
  });

  it("should do abc", () => {
    // Add tests
  });
});

describe("calcBuyXrdSpecified", () => {
  it("should correctly calculate the amount of tokens to receive for the specified amount of XRD", () => {
    // Add tests
  });
});

describe("calcSellTokensSpecified", () => {
  it("should correctly calculate the amount of tokens to receive for the specified amount of XRD", () => {
    // Add tests
  });
});

describe("calcSellXrdSpecified", () => {
  it("should correctly calculate the amount of tokens to receive for the specified amount of XRD", () => {
    // Add tests
  });
});
